name: Ceedling CI

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Change to desired directory
      run: cd master/stm32/UnitTests

    - name: Build and test with Ceedling
      uses: batov/ceedling_action@master
